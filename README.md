# Bagree Diagnostic Centre — Starter React + Tailwind project

This is a ready-to-run Vite + React + Tailwind starter for the Bagree Diagnostic Centre website.

How to use:

1. `npm install`
2. `npm run dev` to start development server
3. `npm run build` to build for production

The project contains a demo `App.jsx` component with search, booking form UI and rate list download.
